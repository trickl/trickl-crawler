# To also add the FileHandler, use the following line instead.
handlers= java.util.logging.ConsoleHandler

# Default global logging level.
# This specifies which kinds of events are logged across
# all loggers.  For any given facility this global level
# can be overriden by a facility specific level
# Note that the ConsoleHandler also has a separate level
# setting to limit messages printed to the console.
.level= INFO

# Limit the message that are printed on the console to INFO and above.
java.util.logging.ConsoleHandler.level = INFO
java.util.logging.ConsoleHandler.formatter = java.util.logging.SimpleFormatter

############################################################
# Facility specific properties.
# Provides extra control for each logger.
############################################################
#com.trickl.crawler.handle.XslTransformHandler.handlers=java.util.logging.ConsoleHandler
#com.trickl.crawler.handle.XslTransformHandler.level=FINEST

#com.trickl.crawler.parser.html.NekoHtmlParser.handlers=java.util.logging.ConsoleHandler
#com.trickl.crawler.parser.html.NekoHtmlParser.level=FINER

